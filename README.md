# transcriptintel
Transcript intel

## Activate conda env

```
$ conda activate crawly
```

(On MacOS: `unset PYTHONPATH`)

## Run analysis

```
$ python -m src.analyze
```

Use cases to do:

1. What if we process video and trimp all filler and pauses (audio processing).

2. Take the transcript.  Find the most important sentences using NLP models and then tie them back together to construct the video summary!  This could be an interesting application :)
